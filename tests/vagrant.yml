---
# tests run by Vagrant
- hosts: all
  remote_user: vagrant
  become: true
  roles:
    - role: ../../
      install: true
      etckeeper_install_tig: true

- hosts: all
  remote_user: vagrant
  become: true
  pre_tasks:
    - name: Add a file
      file:
        path: /etc/foo_added.conf
  roles:
    - role: ../../
      install: false
